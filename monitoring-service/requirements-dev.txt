# Development/test dependencies for the monitoring service.
# Install on top of the runtime requirements:
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt -r requirements-dev.txt
#
# See docs/TESTING.md for running and debugging the suite.

-r requirements.txt

pytest==9.0.3
pytest-cov==5.0.0
